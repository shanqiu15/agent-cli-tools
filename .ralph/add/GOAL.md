# Goal

The local OCR implementation quality is tool low and is not useful. LLM is too expensive, let's implement the google OCR api Google Cloud Vision API support. Find the google's OCR api skd and implement the ocr support. Also see if we can support PDF or we have to add PDF support seperately
