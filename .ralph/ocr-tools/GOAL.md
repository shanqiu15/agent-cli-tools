# Goal

Bootstrap the package and create an OCR tool with a cli interface, the cli can take a image input and output the txt file into an output file (default .txt file), two running mode, either using a local model from huggingface or just run an ORC through llm, 
